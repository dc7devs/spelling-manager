// SEARCH AND COMPARE PROTOTYPE

leveishtein(string1, string2); // return int number

String Search(strBusca, strDictionary) { // Function return a string"
	[...]
	menorQ = [max distance];

	if leveishtein(strBusca, fDicionario[i]) == 0)
		return fDicionario[i]; // dictionary word
	else if leveishtein(string1, string2) < menorQ
		menorQ = leveishtein(string1, string2); // 'menorQ' variable receives distance
	[...]
}